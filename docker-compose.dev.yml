version: "3"
services:
  node-app:
    build:
      context: .
      args:
        - node_env=dev
    volumes:
      - ./src:/app/src:ro
    environment:
      - node_env=dev
    command: npm run start-dev